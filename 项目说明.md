<div align="center">

<img src="frontend/public/youzi-logo.svg" width="96" alt="logo"/>

# Leadhub

</div>


基于 **FastAPI + Vue 3 + TypeScript + Naive UI** 的完整管理系统脚手架。

## 一键启动

```bash
make dev    # 装依赖 + 准备中间件 + 后端 + 前端，Ctrl+C 一起停
```

分开跑（输出更清晰）：`make install` → `make backend-dev`（终端 A）→ `make frontend-dev`（终端 B）。

## 启动后访问

| 服务 | 地址 | 用途 |
|---|---|---|
| 前端 | http://localhost:3000 | 用户界面 |
| 后端 API | http://localhost:8000 | REST 接口 |
| API 文档 | http://localhost:8000/docs | Swagger UI（点 "Authorize" 输入 token 调试） |
| 数据库 UI | http://localhost:8080 | adminer（手动启动：`docker compose --env-file backend/.env up -d adminer`） |
| 账号 | `admin` / 见 `backend/.env` 的 `INITIAL_ADMIN_PASSWORD`（默认 `admin`） | 登录（**生产前必须改密码**） |

> 💡 adminer 登录：Docker 起的 PG 填 Server=`youzi-leadhub-postgres`；复用本机 PG 填 `host.docker.internal`。用户名/密码/库见 `backend/.env`。

## 日常用到的命令

```bash
make help            # 查看完整命令列表
make dev             # 一键启动（装依赖 + 中间件 + 服务）
make install         # 装依赖（后端 venv + 前端 node_modules）
make backend-dev     # 启动后端（自动建表 + 种子）
make frontend-dev    # 启动前端
make test            # 跑后端 + 前端测试
make db-migrate MSG="add order"   # 生成数据库迁移
make db-upgrade      # 应用迁移
make db-downgrade    # ⚠️ 回滚一步迁移
make reset-admin     # 忘了密码？重置
make admin-pass NEW=xxx  # 重置为指定密码
make start           # 启动中间件（优先复用本机已运行的 PG/Redis，缺的用 Docker 起）
make use-sqlite     # 切换 SQLite（零依赖单文件）
make use-pg         # 切换回 PostgreSQL
make stop            # 停止 Docker 中间件容器
```

## 加新业务模块

```bash
python3 backend/scripts/add_module.py order --title "订单管理"
```

> 🤖 用 Claude Code / opencode 继续开发本项目？先让它读根目录 **AGENTS.md**（模块注册 5 处、改字段流程、勿动文件清单全在里面）。

脚本会自动生成 model/schema/crud/router/view/api 六个文件，并提示需要手动注册的 5 处地方。

## 项目结构

```
youzi-leadhub/
├── backend/                # Python 后端（FastAPI）
│   ├── app/                # 应用代码（API/Service/CRUD/Model）
│   ├── alembic/            # 数据库迁移
│   ├── docs/               # 架构/技术栈/配置/开发/API 文档
│   ├── tests/              # 测试
│   ├── scripts/            # 辅助脚本（reset_admin.py / add_module.py）
│   ├── pyproject.toml
│   ├── .env.example
│   └── 后端说明.md
├── frontend/               # 前端（Vue 3 + TS）
│   ├── src/                # 应用代码（api/views/stores/...）
│   ├── docs/               # 架构/技术栈/配置/开发 文档
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── .env.development
├── docker-compose.yml      # PostgreSQL/Redis + adminer（缺中间件时 make start 用 Docker 起）
├── Makefile                # 常用命令
└── 项目说明.md
```

## 文档导航

### 后端
- [架构说明](backend/docs/架构说明.md)
- [配置说明](backend/docs/配置说明.md)
- [开发指南](backend/docs/开发指南.md)
- [API 文档](backend/docs/API文档.md)

### 前端
- [架构说明](frontend/docs/架构说明.md)
- [配置说明](frontend/docs/配置说明.md)
- [开发指南](frontend/docs/开发指南.md)


## License

MIT
