# 线索系统 · 流程时序图与技术方案选型

> 依据：[需求文档.md](需求文档.md)（V1 冻结版，需求唯一真源）+ 当前代码实现（2026-09-01）
> 本文 = 架构视图；业务口径冲突以需求文档为准。

## 1. 需求梳理（一页浓缩）

**核心需求**：作为 WhatsApp 中国 BSP，自动发现「中国企业 ∧ 出海 ∧ 在用 WhatsApp 承接客户」的公司，
评估排序后每天给销售一批可直接建联的客户（卖 SaaS 服务 + WhatsApp 消息，延伸广告代理）。

**两条门 + 一个靶心**：

```
                    ┌───────────── ICP 资格门（硬准入，五态）─────────────┐
6 采集器 → 草稿 → 去重合并 →  qualified/cn_domestic/foreign/non_buyer/unknown
                    └────── 意向分（14 信号加分制，封顶 100，S/A/B/C）──────┘
                                        │
                                   三问生成器（why/what/who 齐备）
                                        │
                                   今日商机批次 → 领取 → 跟进(10态) → won 回传
```

**硬约束（§3，决定一切技术选型）**：纯免费（零付费 API）｜LLM 可空降级｜单进程任务系统（DB 即队列）｜礼貌采集（分窗口节流）｜合规（只采公开信息）｜去重键终态（domain > tel > namecity）。

**系统职责**：不是企业名录，是商机信号工厂——每 1 分意向分可溯源到一条可核查证据；三问齐备才进今日商机；空批次给根因诊断。

---

## 2. 详细流程时序图

### 2.1 主链路全景

```mermaid
sequenceDiagram
    autonumber
    participant Ops as 运营（词表/质检/token）
    participant S as 调度器 APScheduler
    participant R as TaskRunner（asyncio worker ×2）
    participant C as 采集器 6 个
    participant U as upsert_lead（归一化+去重合并）
    participant D as 评分/ICP 同点派生
    participant B as 今日商机批次
    participant Sales as 销售

    S->>R: cron 触发 enqueue(task_id)（pending→queued）
    R->>R: worker 认领 queued（FIFO, UPDATE..RETURNING）
    R->>C: run(TaskContext{params,emit,log})
    C-->>C: meta_ads / b2b_supplier / web_search / job_posting(巡检) / career_site / website_enrich(改存量)
    C->>U: ctx.emit(LeadDraft) 逐条
    U->>U: 归一化(E.164/tldextract/名称) → 三身份列 OR 反查 → 新建或合并
    U->>D: 合并/富化/联系人变更/手工编辑 → 触发重评
    D->>D: 意向分 14 信号相加封顶 100 + 同点派生 icp_status + 出海类型
    Note over R,C: 发现类任务 completed → 自动接力 website_enrich（官网发现→富化）
    B->>B: S/A 新晋 + 高分新增 + 预警切片 → 三问齐备过滤 → 行内三问
    Sales->>B: 领取（原子抢占）→ 跟进（10 态）→ won 回传
```

### 2.2 采集任务生命周期（含自动接力与取消）

```mermaid
sequenceDiagram
    autonumber
    participant Cron as APScheduler（单进程，DB 存 cron）
    participant T as CollectTask 表（DB 即队列）
    participant W as Worker 协程（并发闸门 2）
    participant C as 采集器 Collector.run
    participant L as 线索库

    Cron->>T: _fire：读取任务行（enabled 校验）
    Cron->>W: task_runner.enqueue(id)（仅 pending/cancelled/completed/failed 可入队）
    loop worker 轮询
        W->>T: SELECT 最早 queued（FIFO）→ UPDATE status=running RETURNING id（原子认领）
        alt 无任务
            W->>W: sleep 1s 继续轮询
        end
    end
    W->>W: 注册 cancel_event（setdefault，不覆盖已触发的取消）
    W->>C: asyncio.wait_for(run(ctx), timeout=COLLECT_TASK_TIMEOUT)
    C->>W: set_total / inc_progress（内存计数，5s 节流写库）
    loop 每批条目
        C->>C: ctx.check_cancelled()（取消则抛 CancelledError）
        C->>W: ctx.emit(draft, create_if_missing=巡检开关)
        W->>L: upsert_lead → (lead_id, 是否新建) → 计数 added/merged
    end
    C->>W: ctx.log(level, msg) → CollectTaskLog（增量日志流）
    alt 正常返回
        W->>T: status=completed + 终值进度/计数
        W->>T: 自动接力判定：collector ∈ {web_search,job_posting,meta_ads,b2b_supplier}
        W->>T: 已有 queued/pending 的全库富化？→ 跳过；否则建隐式 website_enrich 任务并入队
    else 超时
        W->>T: status=failed（超时 Ns）
    else 异常
        W->>T: status=failed（类型+消息 ≤1000 字）
    else 取消
        W->>T: status=cancelled
    end
    Note over T,W: 进程重启：start() 把残留 running 全部置 failed（防僵尸）
```

### 2.3 去重合并核心（系统的心脏）

```mermaid
sequenceDiagram
    autonumber
    participant E as ctx.emit(LeadDraft)
    participant N as 归一化
    participant Q as 三身份列 OR 反查
    participant M as 合并语义
    participant DB as leads 表（domain 唯一约束兜底）
    participant S as 重评分

    E->>N: 电话→phonenumbers E.164（来源国兜底，失败不参与去重）
    E->>N: URL→tldextract 可注册域
    E->>N: 公司名→小写去标点剥法律后缀；namecity = 归一名+城市
    N->>Q: 按优先级 domain > tel > namecity > raw 逐键查
    alt 命中已有行
        Q->>M: 合并到最早行（存量电话键 + 新数据域名键是常态）
        M->>M: 标量补空 / 布尔 OR（whatsapp_job 无 wa_ops 证据则重置自愈）
        M->>M: job_urls·社媒并集 / 最近投放·广告数取 max / 来源(线索,来源)唯一 touch
        M->>M: 自动生成联系人（manual 来源除外）
        Q-->>E: (lead_id, False=merged)
    else 未命中且 create_if_missing
        Q->>DB: INSERT（撞 domain 唯一约束 → 转查询合并，并发兜底）
        Q-->>E: (lead_id, True=created)
    else 未命中且巡检模式
        Q-->>E: (0, False)（库外公司不建行）
    end
    M->>S: 触发重评（与采集器同函数，单一口径）
```

### 2.4 website_enrich 富化时序（联系方式是第一产出）

```mermaid
sequenceDiagram
    autonumber
    participant T as website_enrich 任务
    participant F as 官网发现（全库扫描前置）
    participant V as 写前验证
    participant H as 四层抓取
    participant D as 检测器
    participant L as 线索行

    T->>F: 无官网 ∧ 非 foreign → 公司名搜官网（中文名+「官网」词形 / 英文名引号精确）
    F->>V: 候选站写前验证：抓首页标题/域名须含公司名特征词（拉丁≥5字符剔软词/中文相邻汉字对）
    alt 验证通过
        V->>L: 写 website+domain（置信 60）→ 本轮直接富化
    else 验证失败
        V->>V: 负缓存 7 天，不写库（防错站数据污染——爬准红线）
    end
    T->>H: 双通道 HTTP（代理优先+直连兜底）→ TLS 指纹伪装 → 无头浏览器渲染（可选依赖静默降级）
    H->>H: 内页 ≤3，联系类页优先于产品类
    H->>D: 首页 + 内页全量检测
    D->>D: JSON-LD 结构化声明优先（零依赖解析）→ 正则启发兜底
    D->>D: WA（wa.me/api.whatsapp.com/web.whatsapp.com + 插件特征，收集全部号码=多分线证据）
    D->>D: 邮箱（mailto 优先，埋点黑名单防误判）｜电话（国际/400/座机/手机，E.164 校验）
    D->>D: 社媒 5 平台 / 场景与 SaaS 信号 8 类（wa_bsp 竞品栈 18 品牌 raw HTML 指纹）
    D->>L: 邮箱→待补全联系人；WA 前 3→电话联系人；具名联系人（部门+姓名+职务，置信 88，≤6）
    D->>L: enriched_at 只在成功抓首页时写；失败原因线索级标记
    D->>L: 负证据闭环：复查未复现 WA 入口 → whatsapp_gone 事件（≥90 天标注）
    T->>L: 结束日志：成功 X/失败 Y + 分层原因 + 联系方式命中率
```

### 2.5 评分 / ICP / 三问 / 今日商机（同点派生链）

```mermaid
sequenceDiagram
    autonumber
    participant Trig as 触发点（合并/富化/联系人变更/手工编辑）
    participant S as apply_score（单一口径函数）
    participant I as ICP 资格门（同点派生）
    participant Q as 三问生成器
    participant B as 今日商机批次
    participant A as 预警中心

    Trig->>S: score_lead_inputs（14 信号命中→分值相加，封顶 100）
    S->>S: 互斥/防双计：CTWA 替在投 / saas_buying 扣 wa_bsp / whatsapp_job 同事实只计一次
    S->>L: 写 score + grade 列（S80-100/A60-79/B40-59/C0-39）+ score_breakdown 明细
    S->>I: compute_icp_status_of（qualified/cn_domestic/foreign/non_buyer/unknown）
    Note over I: 买家门（黑名单∪词表）→ CN 证据（strong/weak）→ 出海证据；non_buyer 优先
    S->>S: 派生出海业务类型四形态（无证据不硬分类）
    S->>Q: 三问：why=breakdown top3 / what=需求类型A-F+推荐产品 / who=联系人tier→WA号码→角色派生
    Q->>Q: 齐备判定：why≥2 ∧ what≥1 ∧ 真实建联入口任一（目标角色不算）
    B->>B: 批次 = 新晋S/A（等级变更事件）+ 新增高分(qualified∧≥60) + 高价值预警切片
    B->>B: 全量过三问齐备门 → 行内呈现三问
    alt 批次为空
        B-->>B: 空态根因诊断（token 未配置 / 调度开关 / qualified 数）
    end
```

### 2.6 领取与跟进状态机（销售闭环）

```mermaid
sequenceDiagram
    autonumber
    participant Sales as 销售
    participant API as /collect API
    participant DB as leads 表（owner_id + status）
    participant H as 跟进历史（不可变）

    Sales->>API: 领取（claim）——列表行/今日商机/详情头部
    API->>DB: 原子抢占（UPDATE .. WHERE owner IS NULL AND status='unassigned' RETURNING）
    alt 抢占成功
        DB-->>Sales: 持有成功
    else 他人已持有
        DB-->>Sales: 撞单报错（谁持有可见）
    else 自己已持有
        DB-->>Sales: 幂等成功
    end
    Note over DB: 分配族（assign:lead 权限）：主管指派 / 释放 / 自动分配（共享池按分轮转+负载上限）
    Sales->>API: 推进跟进（一次一条不可变历史）
    Note over DB: 10 态：unassigned→pending→contacted→replied→opportunity→quote→negotiation→won | invalid(终态) | paused
    alt won / invalid
        DB->>DB: 终态退出富化范围与重爬；成交数据回传（未来加权模型燃料）
    end
```

### 2.7 质量抽检（质检闭环）

```mermaid
sequenceDiagram
    autonumber
    participant QA as 质检员
    participant API as /quality
    participant Pool as 抽样池（随机/已标不重复/overseas 维弱 CN 优先/contact 池含电话联系人）
    participant KPI as 指标看板

    QA->>API: 抽一批（三维度：WhatsApp / 出海 / 联系人）
    API->>Pool: 出队带核验证据的线索
    QA->>API: 标注 正确/错误/拿不准（每线索取最新标注，拿不准不进分母）
    API->>KPI: 三准确率 vs 目标线 90%/80%/60% + S+A 占比 vs 20% + 覆盖进度
    Note over Pool: 漏网案例 → 增补买家黑名单/词表（两处同源维护，入库前拦截+存量兜底）
```

---

## 3. 技术方案选型

### 3.1 后端基础设施

| 关注点 | 选型 | 依据 | 被否决备选 |
|---|---|---|---|
| Web 框架 | **FastAPI + Pydantic v2 + SQLAlchemy 2 (async)** | 异步原生、参数表单动态渲染靠 Pydantic 模式、类型安全 | Django（重）/ Flask（异步弱） |
| 数据库 | **PostgreSQL (asyncpg) 主 / SQLite (aiosqlite) 零配置备选**（`DB_TYPE` 切换） | PG 支持 `UPDATE..RETURNING` 原子认领与 JSON 查询；SQLite 3.35+ 同支持，纯本地体验零依赖 | MySQL（无 RETURNING 语义弱） |
| 迁移 | **Alembic autogenerate** | 与 SQLAlchemy 同生态 | 手写 SQL |
| 任务队列 | **DB 即队列（CollectTask 表 + asyncio worker 协程）** | §3 硬约束单进程；重启不丢任务；无新中间件（纯免费/零依赖） | Celery/Redis/RQ（违反单进程、增依赖） |
| 调度 | **APScheduler AsyncIOScheduler，cron 表达式存 DB，sync() 全量重建** | 任务即数据（前端可建可改）；重启自动恢复；`WORKERS>1` 时防御性关闭 | 系统 crontab（不可前端管理） |
| 并发模型 | **进程内 worker 协程 × `COLLECT_MAX_CONCURRENT`（默认 2）** | 闸门满即排队不失败；进度 5s 节流写库 | 线程池（GIL + 异步生态割裂） |
| 日志 | **loguru + CollectTaskLog 增量日志表** | 任务详情页日志流；重启可追溯 | 仅文件日志（前端不可见） |
| 认证授权 | **JWT + bcrypt + 权限码词表（7 个）** | 三层权限（lead/task/assign）；数据权限三级（all/team/own）强制过滤 | OAuth2 第三方（内网工具过度） |

### 3.2 采集层（纯免费硬约束下的全部通道）

| 采集器 | 选型 | 依据 | 被否决备选 |
|---|---|---|---|
| meta_ads | **Meta 官方 Ad Library API（免费 token）**，httpx 异步 | 唯一免费合法通道拿「在投广告」；CTWA(+40)/在投(+15)/投放国计数都靠它 | 第三方广告库 API（付费） |
| web_search | **DuckDuckGo（零 key）→ 失败自动降级必应中国 → 可选 searxng/google_cse/bing** | 零 key 可用；多引擎降级链保活 | SerpAPI 等（付费，违反 §3） |
| b2b_supplier | **made-in-china.com HTTP 直连解析（robots 允许目录页/店铺页/档案页）** | 国内直连零 key；公开字段全（出口市场/Incoterms）；适配器模式可扩展其他目录 | 海关数据（付费/灰色） |
| job_posting | **jobui/猎聘/前程无忧 HTTP 解析；BOSS/智联 STUB（验证码，归 V1.5）** | 中国招聘平台公开职位页；巡检定位（信号不是发现） | 招聘 API 官方付费 |
| career_site | **官网 + ATS 子域（Moka/北森/51job）递归发现 + 薄壳 SPA 渲染 + 分类器过滤** | 一手在招岗证据置信 90 | 第三方招聘聚合 |
| website_enrich | **双通道 httpx（代理优先+直连兜底）→ TLS 指纹伪装 → 无头浏览器可选降级** | 反爬对抗分层递进；未装浏览器依赖静默降级不炸任务 | Scrapy（重框架）；付费浏览器农场 |
| 归一无 | **phonenumbers (libphonenumber) + tldextract + 自研名称归一** | E.164 校验是去重键质量前提 | libpostal（实证失败不引入，§10 裁决） |
| 结构化抽取 | **stdlib json 解析 JSON-LD 优先 + 正则兜底** | 零依赖采信 schema.org；extruct/trafilatura 实证后否决 | 完整微数据解析栈 |
| 反爬伪装 | **TLS 指纹 impersonate + 分窗口节流（S1/A3/B7/C30 天）+ 终态退出** | 礼貌采集（§3.4）；分级增量重爬 | 高频全量重爬 |

### 3.3 智能判定层

| 关注点 | 选型 | 依据 | 被否决备选 |
|---|---|---|---|
| LLM 接入 | **可空降级：OpenAI 兼容 client + 本机 Ollama 兜底；无 key 全走规则模板并标记来源** | §3.2 LLM 可空；AI 分析/话术是增强不是依赖 | 强制在线 LLM（有 key 才工作 = 违约） |
| 评分模型 | **加分制 14 信号，命中分值相加封顶 100，唯一主分** | 零成交数据时加权只会放大空通道（§10 裁决）；每分可溯源 | 六维加权（已废弃，成交回传后再说） |
| ICP 资格门 | **规则判定五态（买家门→CN 证据 strong/weak→出海证据）** | 硬准入不做有罪推定；weak CN 可见不可藏 | LLM 语义判定（V1.5 才入，且为本机） |
| 三问生成 | **规则派生（breakdown top3 / 需求类型 A-F / 联系人 tier + 角色派生）** | 确定性可解释；LLM 仅润色话术 | 纯 LLM 生成（不可核查 = 违反「每分可溯源」） |

### 3.4 业务核心机制

| 关注点 | 选型 | 依据 | 被否决备选 |
|---|---|---|---|
| 去重键 | **三身份列 domain > tel > namecity（终态）；DB 唯一约束兜底** | 宁漏勿重；并发/发现链/合并三条撞域路径都无重复 | 统一社会信用代码（永久废弃）；模糊匹配（误合并） |
| 领取并发 | **条件 UPDATE 原子抢占（`WHERE owner IS NULL` RETURNING）** | 两人同时领取恰好一人成功；幂等 | 事务锁/应用层先查后改（竞态） |
| 跟进历史 | **不可变追加（一次跟进一条记录）** | 审计可回溯；won/invalid 终态 | 可编辑状态字段（丢历史） |
| 空态诊断 | **今日商机空批次输出根因（token/调度/qualified 数）** | §1「批次为空给根因不给空白」 | 空白页 |
| 错配防线 | **官网两证齐判错 + 连坐清除 + 写前验证（公司名特征词）** | §5 可行动底线；爬准红线（错站数据全清） | 单证判定（误杀/漏放） |
| 数据进出 | **CSV UTF-8 BOM（≤5 万行导出 / ≤5000 行导入 upsert）；种子初始化在空库时一次性导入** | Excel 兼容；幂等重导 | Excel 二进制格式（依赖重） |
| 权限 | **三层 RBAC + 7 权限码 + 数据权限 all/team/own** | V1 只接线 assign:lead 与 stats:read，余为扩展预留 | 逐功能硬编码角色 |

### 3.5 前端

| 关注点 | 选型 | 依据 | 被否决备选 |
|---|---|---|---|
| 框架 | **Vue 3 + TypeScript + Vite** | 脚手架同源（youzi-init-project 硬约束） | React（迁移成本） |
| 组件库 | **naive-ui（unplugin 按需导入）唯一** | 生态硬约束；n-data-table/n-modal/n-config-provider 覆盖全部页面 | Element Plus/AntDV（明令禁止） |
| 图标 | **@vicons（xicons 生态）+ n-icon/renderIcon** | 生态硬约束 | 其他图标库（禁止） |
| 请求 | **@/api/request 统一封装（token 自动带、401 统一弹窗）** | 约定；不手写 fetch | 各页面裸 fetch |
| 确认/提示 | **confirm()（@/utils/feedback）+ message.*** | 约定统一交互 | window.confirm |

### 3.6 关键决策记录（为什么这么选）

1. **DB 即队列而非 MQ**：§3 硬约束「单进程任务系统」+「纯免费」双重否决 Celery/Redis；`UPDATE..RETURNING` 让 PG/SQLite 都支持原子认领，重启靠 `start()` 把 running 置 failed 兜底。
2. **加分制而非加权**：零成交数据时权重纯靠拍脑袋，会放大空通道（2026-08-31 裁决）；加分制每分可溯源，销售直读「这分怎么来的」。
3. **富化不是数据源**：website_enrich 只改存量、不产新线索，因此不在数据源列表/创建对话框（2026-09-01 裁决）；自动接力让它成为发现链的隐式第二步。
4. **LLM 是彩蛋不是地基**：无 key 全链路规则模板可用；Ollama 本机兜底保证「纯免费」不变。
5. **官网必须真是这家公司的**：英文名搜索结果第一站无关站曾致错配联系方式入库——写前验证（公司名特征词）+ 两证判错 + 连坐清除三道防线，这是全系统数据质量的根。
